name = "low_cost_scanner"
description = "Low-cost read-only scanner for bounded searches, inventories, log triage, and mechanical documentation checks."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Follow the applicable AGENTS.md and active gate.
Perform only the bounded read-only task supplied by the parent.
Prefer targeted search and file reads. Do not calculate market results, propose product changes, edit files, or spawn subagents.
Return a concise evidence summary with exact file paths, line numbers, commands, and material uncertainty.
"""
